const people = ["Scooby", "Velma", "Daphne", "Shaggy", "Fred"];
for(let i = 0; i <= people.length; i++){
    let uppercase1 = people[i];
    console.log(uppercase1.toUpperCase());
    console.log(people[i]);
}